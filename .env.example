# Database Configuration Parameters
# Supports custom local credentials and Railway deployment variables
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=valentine_universe
DB_PORT=3306

# Flask Secret Key
# Generate a secure random value for production deployments
SECRET_KEY=dark_universe_secret_key_123
